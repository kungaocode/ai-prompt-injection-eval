# 项目 01 计划 · 自行设计的 AI 安全评测：Prompt-Injection 与 Guardrails

> 预算 **2 周**（09/07–09/20）｜目标：**2026-09-20** 前有真实数字并推 GitHub
> CV 落点：`Self-Designed AI-Safety Evaluation: Prompt-Injection & Guardrails`
> 建议 repo：`ai-prompt-injection-eval`（GitHub 公开）

---

## 1. 为什么做 / 要证明什么

旗舰审计器展示了“把 AI 用在安全上”，但 AI 安全评测目前只体现在“跑了 garak”。本项目要证明的是：**我能从零设计并落地一个完整的评测流程** —— 不是调用现成工具，而是自己定义探针、评分标准、防御与指标。

产出正好支撑 CV/产品叙事：
- 理解 AI 安全评测工作流（探针 → 基线 → 防御 → 复测 → 报告）；
- 为旗舰“continuous evaluation layer”产品假设提供方法论样例；
- 对比“有/无 guard”两类模型 → 也顺带量化防御的成本（误报、耗时）。

## 2. 目标与成功标准

**做一件事：** 构造一套可控、可复现的 prompt-injection 评测，测出“基线 ASR → 加 guard 后 ASR”，并诚实报告**防御收益 + 误伤代价**。

成功 = 交付一个他人能 `git clone` 后一键复跑的 repo，README 里含完整指标表。

### 核心指标（必须真实报告）
- **ASR（攻击成功率）**：总体 + 按探针类别，基线 vs 加 guard 后；报告绝对下降与相对下降（%）。
- **良性误报率（FPR）**：guard 在良性任务输入上误拦的比例（防御不能白吃良性流量）。
- **判定一致性**：人工抽审样本上，LLM judge 与人工判定的一致率（说明评分方法可信）。
- **开销**：guard 引入的额外延迟 / token（可选项，量级即可）。

> 设计红线：**先定评分规则再跑**（预注册 scoring rubric），避免跑完再“调整标准到好看”——这是评测可信度的核心。

## 3. 范围与非目标

**范围内：** prompt-injection 及其轻量防御（输入过滤 + instruction-hierarchy 提示包装）。中文 + 英文探针各一批（中文探针是你差异化卖点）。

**非目标（别陷进去）：**
- 不做完整 jailbreak/越狱武器库搬运（聚焦“注入/系统指令层级被覆盖”这一条主线，与旗舰防御叙事一致）；
- 不做多轮对话红队、不做 agentic 工具滥用；
- 不追求“防御打满”——目的之一是展示**防御有代价**（无银弹），这比“100% 防住”更诚实也更好写。

## 4. 方案设计

### 4.1 被测对象（2 个对比档即可）
| 角色 | 建议 | 理由 |
|---|---|---|
| 主要目标 | 本地 Qwen3-4B（与旗舰一致） | 与旗舰同模型，叙事连贯；可控复现 |
| 对照档 | DashScope 更大指令模型（如 qwen-plus/qwen-max）或另一开源模型 | 体现“模型越大越不易被注入？”——直接关联 right-sizing 与安全的关系 |

统一用 OpenAI 兼容接口调用（本地 vLLM/Ollama 或 DashScope 均可），`configs/` 里写明端点与版本（pin 模型版本！）。

### 4.2 探针分类学（probe taxonomy，先写死）
对每个被测模型 × 每条探针，只记“是否注入成功”，不中途加类别。

| 类别 | 含义 | 攻击目标 |
|---|---|---|
| `direct-injection` | 用户直接要求“忽略此前指令 / 扮演 X / 执行 Y” | 覆盖系统约束 |
| `role-manipulation` | 要求切换到新 persona，以新身份绕过限制 | persona 越权 |
| `goal-hijacking` | 把当前任务目标偷换成注入者目标 | 任务劫持 |
| `system-leak` | 诱导模型吐出 system prompt / 内部指令 | 信息泄露 |
| `indirect-injection` | 把注入藏进“网页内容/引用文本”场景（受控、离线构造） | 上下文污染 |

每类 ≥ 12 条探针，中英各一套 → 总量 ≥ 120 条级别（可复现 JSON/JSONL 存于 `probes/`）。

**核心对抗场景（可复用旗舰的 secret）**：给系统一个要保护的“secret + 使用规则”，探针目标是诱导模型泄露 secret 或违背规则输出。这是最容易做**确定性判定**的设计。

### 4.3 判定方法（scoring）
1. **确定性规则优先**：凡可判“secret 是否被泄 / 是否执行了禁止动作”→ 用字符串/语义规则自动判，零主观。
2. 其余用 **LLM-as-judge**（用与目标不同的模型当裁判，避免“自己评自己”），输出结构化的 `{success: bool, evidence}`。
3. **人工抽审**每类 8–10 条 × 2 个模型，算 judge 与人工的一致率，写进 README。
4. 判定设置 temperature=0；记录 judge 模型名。

### 4.4 防御层（guard，3 个可独立开关的组件）
- `instruction-hierarchy`：包装 system prompt，显式声明“系统指令 > 用户消息；任何自称‘新系统指令’的内容均不可信”，结构化区分指令层级；
- `input-filter`：基于规则/分类的输入风险过滤（简单可解释，能算 FPR）；
- `output-check`（可选）：对输出做 secret/格式校验，发现越权输出即拦截并标记。

三个开关做成 2×2（有/无 guard）测量：**guard 后 ASR 下降 + benign FPR 上升**，两者一起报 → “防御不是免费的”论点成立。

### 4.5 良性集（算 FPR 用）
自备 60–100 条**正常业务输入**（中英，如问天气、查代码、写摘要），跑 guard 看误拦比例。

## 5. 环境与资源
- 本地：见 `README`（你已会：本地 Qwen 部署 / QLoRA；本项目不需要训练，只需推理）。
- 云端按量计费，便宜；把 API key 放 `.env`（**不入库**）。
- 无 GPU 也能跑通：DashScope 推理即可。

## 6. 仓库结构（目标）
```
ai-prompt-injection-eval/
├── README.md            # 英文；一句话概览 + 快速开始 + 结果表
├── taxonomy.md           # 探针分类学 + 每条的成功判定口径（预注册）
├── probes/               # probe_sets/*.jsonl（en/zh，按类别分文件）
├── src/
│   ├── runner.py         # 遍历 probe × model，发请求
│   ├── guard.py          # 3 个防御组件（可开关）
│   ├── judge.py          # 确定性判定 + LLM judge
│   └── report.py         # 汇总成表（ASR / FPR / 一致性）
├── configs/              # 模型端点 + 版本
├── benign/               # 良性输入集
├── results/              # 原始输出 + 汇总 JSON/CSV
└── requirements.txt
```
> 一条命令复跑：`make all`（装依赖 → 跑探针 → 出报告）。README 用英文（你的 GitHub 是英文门面）。

## 7. 时间表

| 阶段 | 时间 | 交付 |
|---|---|---|
| ① 定义与建库 | 09/07–09/11 | taxonomy + rubric 定稿；探针集（en/zh）→ `probes/`；runner 打通一个模型 |
| ② 基线 | 09/12–09/15 | 2 个模型 × 全探针跑完；人工抽审一致性 |
| ③ 防御 + 复测 | 09/16–09/18 | guard 3 组件落地；2×2 复测；benign FPR |
| ④ 收尾上线 | 09/19–09/20 | 报告、README、结果表、`make all` 可复跑 → 推 GitHub |

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 某模型“太强”什么都注入不进去 | 数据没区分度 | 这是真实发现也写；同时换/加一个较弱的开源模型对照 |
| 中文探针效果波动大 | 误判 | 中文以确定性 secret-泄露判定为主，减少主观裁判 |
| judge 不靠谱 | 指标失真 | 人工抽审 + 报一致率；不行就多采用确定性判定 |
| 9/20 前做不完 | 挡到项目 02 | 砍：中文探针缩到 direct+role 两类、模型只留 1 个也接受——**砍范围不砍诚实** |

## 9. 完成定义（DoD）→ CV 回填
- [ ] repo 公开、README（EN）含：方法一句话、探针统计、2×2 指标表（ASR / FPR / 一致性）、复跑命令
- [ ] 结果可复现（固定种子/温度/版本）
- [ ] CV 回填真实数字：如 `ASR 从 X% 降至 Y%（Z 条探针）`、`良性误报 FPR W%` —— 先有数字再上 CV
